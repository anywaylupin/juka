# Session secret for nuxt-auth-utils. Generate with: openssl rand -base64 32
NUXT_SESSION_PASSWORD=

# Only needed for `wrangler --remote` commands and deploys.
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=
